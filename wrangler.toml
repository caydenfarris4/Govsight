name = "govsight"
compatibility_date = "2026-07-01"

# Cloudflare Workers serves the static front-door and self-contained tools
# in public/ as a static-assets deployment (`npx wrangler deploy`).
# The full Streamlit platform is containerized separately (see Dockerfile
# and documentation/DEPLOYMENT.md).
[assets]
directory = "./public"

// GENERATED by scripts/extract_frontend_tools.py - edit the source HTML, then re-run.
import './investmentOptimizer.css';

const BODY_HTML = '<div class="container">\n        <div class="header">\n            <h1>Municipal Investment Optimizer</h1>\n            <p>Safe, accredited investment opportunities for municipal cash reserves</p>\n        </div>\n\n        <div class="tabs" id="tabs">\n            <button class="tab active" onclick="switchTab(\'opportunities\')">Investment Opportunities</button>\n            <button class="tab" onclick="switchTab(\'calculator\')">Cash Optimization</button>\n            <button class="tab" onclick="switchTab(\'comparison\')">Yield Comparison</button>\n            <button class="tab" onclick="switchTab(\'portfolio\')">Portfolio</button>\n            <button class="tab" onclick="switchTab(\'grants\')">Grant + Investment</button>\n            <button class="tab" onclick="switchTab(\'education\')">Educational Resources</button>\n        </div>\n\n        <div class="content">\n            <div id="opportunities" class="tab-content active">\n                <!-- Content will be generated by JavaScript -->\n            </div>\n\n            <div id="calculator" class="tab-content">\n                <h2 style="margin-bottom: 20px">Cash Optimization Calculator</h2>\n                <p style="color: #5b6b7a; margin-bottom: 30px">\n                    Enter your available cash and investment criteria to get personalized recommendations.\n                </p>\n\n                <div class="grid-2">\n                    <div class="input-group">\n                        <label class="input-label">Available Cash to Invest</label>\n                        <input type="number" class="input-field" id="calc-principal" value="500000" min="100" step="10000">\n                    </div>\n\n                    <div class="input-group">\n                        <label class="input-label">Liquidity Requirement</label>\n                        <select class="input-field" id="calc-liquidity">\n                            <option value="any">Any</option>\n                            <option value="high">High (daily access)</option>\n                            <option value="low">Low (can lock for term)</option>\n                        </select>\n                    </div>\n                </div>\n\n                <button class="btn btn-primary" onclick="calculateBest()">Find Best Investment</button>\n\n                <div id="calc-result"></div>\n            </div>\n\n            <div id="comparison" class="tab-content">\n                <h2 style="margin-bottom: 20px">Investment Yield Comparison</h2>\n\n                <div class="input-group">\n                    <label class="input-label">Amount to Compare</label>\n                    <input type="number" class="input-field" id="compare-amount" value="1000000" min="1000" step="100000" onchange="updateComparison()">\n                </div>\n\n                <div class="chart-container">\n                    <canvas id="comparisonChart"></canvas>\n                </div>\n\n                <div id="comparison-table"></div>\n            </div>\n\n            <div id="portfolio" class="tab-content">\n                <h2 style="margin-bottom: 6px">Investment Portfolio</h2>\n                <p style="color:#5b6b7a; margin-bottom: 16px">Track actual holdings: maturity ladder, weighted yield, reinvestment alerts, and the cost of idle cash.</p>\n                <div id="portfolio-status"></div>\n                <div class="metrics" id="portfolio-metrics"></div>\n                <div class="grid" style="display:grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px">\n                    <div class="card" style="background:#fff; border:1px solid #dde4ea; border-radius:10px; padding:16px">\n                        <h3 style="margin-bottom:10px">Maturity Ladder</h3>\n                        <div style="height:260px"><canvas id="ladder-chart"></canvas></div>\n                    </div>\n                    <div class="card" style="background:#fff; border:1px solid #dde4ea; border-radius:10px; padding:16px">\n                        <h3 style="margin-bottom:10px">Idle Cash Opportunity Cost</h3>\n                        <div class="input-group"><label>Idle cash in checking (earning ~0%)</label>\n                            <input type="number" id="idle-cash" value="0" step="100000"></div>\n                        <div class="input-group"><label>Benchmark rate (best liquid option, %)</label>\n                            <input type="number" id="benchmark-rate" value="4.5" step="0.05"></div>\n                        <button class="button" onclick="renderPortfolio()">Recalculate</button>\n                        <div id="opp-cost-result" style="margin-top:12px"></div>\n                    </div>\n                </div>\n                <div id="maturity-alerts"></div>\n                <h3 style="margin:16px 0 8px">Holdings</h3>\n                <div id="holdings-table"></div>\n                <h3 style="margin:20px 0 8px">Add Holding</h3>\n                <div style="display:grid; grid-template-columns: repeat(3, 1fr); gap:10px; max-width: 900px">\n                    <div class="input-group"><label>Type</label>\n                        <select id="h-type"><option value="t-bill">T-Bill</option><option value="cd">CD (CDARS/ICS)</option><option value="lgip">LGIP</option><option value="mmf">Money Market Fund</option><option value="agency">Agency</option><option value="other">Other</option></select></div>\n                    <div class="input-group"><label>Description</label><input type="text" id="h-desc" placeholder="13-week T-Bill, July auction"></div>\n                    <div class="input-group"><label>Principal ($)</label><input type="number" id="h-principal" step="10000"></div>\n                    <div class="input-group"><label>Rate (%)</label><input type="number" id="h-rate" step="0.001"></div>\n                    <div class="input-group"><label>Purchase date</label><input type="date" id="h-purchase"></div>\n                    <div class="input-group"><label>Maturity date (blank = liquid)</label><input type="date" id="h-maturity"></div>\n                </div>\n                <button class="button" onclick="addHolding()">Add Holding</button>\n            </div>\n\n            <div id="grants" class="tab-content">\n                <h2 style="margin-bottom: 20px">Grant + Investment Integration</h2>\n                <p style="color: #5b6b7a; margin-bottom: 30px">\n                    Maximize returns on grant funds between receipt and expenditure dates.\n                </p>\n\n                <div class="grid-2">\n                    <div class="input-group">\n                        <label class="input-label">Grant Amount</label>\n                        <input type="number" class="input-field" id="grant-amount" value="500000" min="1000" step="10000">\n                    </div>\n\n                    <div class="input-group">\n                        <label class="input-label">Days Until Expenditure</label>\n                        <input type="number" class="input-field" id="grant-days" value="90" min="1" max="365">\n                    </div>\n                </div>\n\n                <button class="btn btn-primary" onclick="optimizeGrant()">Optimize Grant Investment</button>\n\n                <div id="grant-result"></div>\n            </div>\n\n            <div id="education" class="tab-content">\n                <h2 style="margin-bottom: 20px">Municipal Investment Education</h2>\n\n                <div style="margin-bottom: 30px">\n                    <h3 style="color: #12263a; margin-bottom: 16px">Understanding Safe Investment Options</h3>\n                    <p style="color: #5b6b7a; line-height: 1.6">\n                        Cities and municipalities have several accredited and regulated investment options for managing cash reserves.\n                    </p>\n                </div>\n\n                <div class="opportunity-card">\n                    <h4 style="color: #12263a; margin-bottom: 12px">US Treasury Securities</h4>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>What:</strong> Direct obligations of the US Government\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>Types:</strong> Treasury Bills (4 weeks to 1 year), Treasury Notes (2 to 10 years), Treasury Bonds (20 to 30 years)\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>Safety:</strong> Backed by full faith and credit of US Government (AAA rated)\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 16px">\n                        <strong>Best for:</strong> Risk-free investment, benchmark for other rates\n                    </p>\n                    <div style="display: flex; gap: 12px; flex-wrap: wrap">\n                        <a href="https://www.treasurydirect.gov/" target="_blank" class="btn btn-primary" style="text-decoration: none">\n                            Treasury Direct →\n                        </a>\n                        <a href="https://www.treasurydirect.gov/auctions/upcoming/" target="_blank" class="btn btn-secondary" style="text-decoration: none">\n                            View Auctions →\n                        </a>\n                    </div>\n                </div>\n\n                <div class="opportunity-card">\n                    <h4 style="color: #12263a; margin-bottom: 12px">CDARS / ICS (IntraFi Network)</h4>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>What:</strong> Certificate of Deposit Account Registry Service / IntraFi Cash Service\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>How it works:</strong> Your deposit is split into increments under $250,000 and placed at different FDIC-insured banks\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>Network:</strong> 3,000+ participating banks nationwide\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 16px">\n                        <strong>Best for:</strong> Large cash balances requiring FDIC insurance\n                    </p>\n                    <div style="display: flex; gap: 12px; flex-wrap: wrap">\n                        <a href="https://www.intrafi.com/" target="_blank" class="btn btn-primary" style="text-decoration: none">\n                            IntraFi Network →\n                        </a>\n                        <a href="https://www.intrafi.com/find-a-network-bank" target="_blank" class="btn btn-secondary" style="text-decoration: none">\n                            Find Your Bank →\n                        </a>\n                    </div>\n                </div>\n\n                <div class="opportunity-card">\n                    <h4 style="color: #12263a; margin-bottom: 12px">Government Money Market Funds</h4>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>What:</strong> Mutual funds investing in government securities\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>Types:</strong> Treasury MMF (99.5%+ in Treasury securities), Government MMF (99.5%+ in government securities)\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>Safety:</strong> AAA rated, highly stable $1.00 NAV\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 16px">\n                        <strong>Best for:</strong> Operating cash needing daily liquidity\n                    </p>\n                    <div style="display: flex; gap: 12px; flex-wrap: wrap">\n                        <a href="https://investor.vanguard.com/investment-products/mutual-funds/profile/vusxx" target="_blank" class="btn btn-primary" style="text-decoration: none">\n                            Vanguard VUSXX →\n                        </a>\n                        <a href="https://www.schwab.com/public/schwab/investing/accounts_products/investment/money_markets_funds" target="_blank" class="btn btn-secondary" style="text-decoration: none">\n                            Schwab MMFs →\n                        </a>\n                    </div>\n                </div>\n\n                <div class="opportunity-card">\n                    <h4 style="color: #12263a; margin-bottom: 12px">Local Government Investment Pools (LGIPs)</h4>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>What:</strong> State-sponsored investment pools for local governments\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>Structure:</strong> Managed by state treasury, invests in high-quality short-term securities\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 12px">\n                        <strong>Safety:</strong> State-regulated, typically AAA/AAAm rated\n                    </p>\n                    <p style="color: #5b6b7a; margin-bottom: 16px">\n                        <strong>Best for:</strong> Local government entities, competitive rates with safety\n                    </p>\n                    <div style="display: flex; gap: 12px; flex-wrap: wrap">\n                        <a href="https://www.gfoa.org/materials/local-government-investment-pools" target="_blank" class="btn btn-primary" style="text-decoration: none">\n                            GFOA Resources →\n                        </a>\n                        <a href="https://www.msrb.org/LGIP-Investment-Pool-Structure" target="_blank" class="btn btn-secondary" style="text-decoration: none">\n                            MSRB Guide →\n                        </a>\n                    </div>\n                </div>\n\n                <div class="alert alert-info" style="margin-top: 30px; background: #dbeafe; border: 1px solid #bfdbfe; color: #1e40af">\n                    \n                    <div>\n                        <strong>Additional Resources:</strong>\n                        <ul style="margin-top: 8px; margin-left: 20px">\n                            <li><a href="https://www.gfoa.org/" target="_blank" style="color: #1e40af; text-decoration: underline">Government Finance Officers Association (GFOA)</a></li>\n                            <li><a href="https://www.msrb.org/" target="_blank" style="color: #1e40af; text-decoration: underline">Municipal Securities Rulemaking Board (MSRB)</a></li>\n                            <li><a href="https://www.fdic.gov/" target="_blank" style="color: #1e40af; text-decoration: underline">Federal Deposit Insurance Corporation (FDIC)</a></li>\n                        </ul>\n                    </div>\n                </div>\n            </div>\n        </div>\n    </div>';


        // Static fallback rates (October 2025 snapshot). These are placeholders
        // used ONLY when neither server-injected rates nor the platform API is
        // available; every entry is labeled static in the UI.
        const STATIC_OPPORTUNITIES = [
            {
                id: 'treasury-4w',
                name: 'US Treasury T-Bill - 4 weeks',
                provider: 'US Treasury',
                type: 'Government Security',
                rate: 5.25,
                termDays: 28,
                termDisplay: '4 weeks',
                minimum: 100,
                safetyRating: 'AAA',
                fdic: false,
                govt: true,
                liquidity: 'High',
                source: 'STATIC SNAPSHOT (Oct 2025) - live rate unavailable',
                isLive: false,
                link: 'https://www.treasurydirect.gov/auctions/upcoming/',
                linkText: 'Buy T-Bills at Treasury Direct'
            },
            {
                id: 'treasury-13w',
                name: 'US Treasury T-Bill - 13 weeks',
                provider: 'US Treasury',
                type: 'Government Security',
                rate: 5.35,
                termDays: 91,
                termDisplay: '13 weeks',
                minimum: 100,
                safetyRating: 'AAA',
                fdic: false,
                govt: true,
                liquidity: 'High',
                source: 'STATIC SNAPSHOT (Oct 2025) - live rate unavailable',
                isLive: false,
                link: 'https://www.treasurydirect.gov/auctions/upcoming/',
                linkText: 'Buy T-Bills at Treasury Direct'
            },
            {
                id: 'mmf-vanguard',
                name: 'Vanguard Treasury Money Market Fund (VUSXX)',
                provider: 'Vanguard',
                type: 'Treasury Money Market Fund',
                rate: 4.30,
                termDays: 1,
                termDisplay: 'Daily liquidity',
                minimum: 3000,
                safetyRating: 'AAAm',
                fdic: false,
                govt: true,
                liquidity: 'Daily',
                source: 'ESTIMATED RATE (Oct 2025: 4.13-4.34%) - Verify at investor.vanguard.com/vusxx',
                isLive: false,
                notes: 'State tax exempt - distributions exempt from state and local income tax',
                link: 'https://investor.vanguard.com/investment-products/mutual-funds/profile/vusxx',
                linkText: 'Open Account at Vanguard'
            },
            {
                id: 'cdars-3m',
                name: 'CDARS CD - 3 Month',
                provider: 'IntraFi Network (CDARS)',
                type: 'Certificate of Deposit',
                rate: 4.00,
                termDays: 90,
                termDisplay: '3 months',
                minimum: 250000,
                safetyRating: 'FDIC Insured',
                fdic: true,
                govt: false,
                liquidity: 'Low (penalty for early withdrawal)',
                source: 'ESTIMATED (Oct 2025 market: 3.95-4.50%) - Contact your IntraFi network bank',
                isLive: false,
                notes: 'Multi-million FDIC insurance through network of 3,000+ banks. Rates typically 0.25-0.50% below standard CDs',
                link: 'https://www.intrafi.com/find-a-network-bank',
                linkText: 'Find IntraFi Network Bank'
            },
            {
                id: 'cdars-12m',
                name: 'CDARS CD - 12 Month',
                provider: 'IntraFi Network (CDARS)',
                type: 'Certificate of Deposit',
                rate: 4.15,
                termDays: 365,
                termDisplay: '12 months',
                minimum: 250000,
                safetyRating: 'FDIC Insured',
                fdic: true,
                govt: false,
                liquidity: 'Low (penalty for early withdrawal)',
                source: 'ESTIMATED (Oct 2025 market: 3.95-4.42%) - Contact your IntraFi network bank',
                isLive: false,
                notes: 'Multi-million FDIC insurance through network of 3,000+ banks. Rates typically 0.25-0.50% below standard CDs',
                link: 'https://www.intrafi.com/find-a-network-bank',
                linkText: 'Find IntraFi Network Bank'
            },
            {
                id: 'ics',
                name: 'ICS Money Market',
                provider: 'IntraFi Network (ICS)',
                type: 'Money Market Account',
                rate: 4.00,
                termDays: 1,
                termDisplay: 'Daily liquidity',
                minimum: 250000,
                safetyRating: 'FDIC Insured',
                fdic: true,
                govt: false,
                liquidity: 'High (daily access)',
                source: 'ESTIMATED (Oct 2025 range: 2.85-4.05%) - Contact your IntraFi network bank',
                isLive: false,
                notes: 'Multi-million FDIC insurance with daily liquidity. Rates vary significantly by bank and balance tier',
                link: 'https://www.intrafi.com/find-a-network-bank',
                linkText: 'Find IntraFi Network Bank'
            },
            {
                id: 'lgip-liquid',
                name: 'State LGIP - Liquid Pool',
                provider: 'State Investment Pool',
                type: 'Local Government Investment Pool',
                rate: 5.00,
                termDays: 1,
                termDisplay: 'Daily liquidity',
                minimum: 100000,
                safetyRating: 'AAA/AAAm',
                fdic: false,
                govt: false,
                liquidity: 'Daily',
                source: 'ESTIMATED (Oct 2025 national avg: 4.99-5.08%) - Contact your state treasury',
                isLive: false,
                notes: 'State-regulated investment pool for local governments. Rates vary by state - check with your state treasurer',
                link: 'https://www.gfoa.org/materials/local-government-investment-pools',
                linkText: 'Find Your State LGIP'
            }
        ];

        let comparisonChart = null;

        // Calculate investment return
        let OPPORTUNITIES = STATIC_OPPORTUNITIES.slice();
        let DATA_STATUS = { mode: 'static', generatedAt: null, staleCount: STATIC_OPPORTUNITIES.length };

        const API_BASE = (typeof window !== 'undefined' && window.GOVSIGHT_API_URL) || '';

        function mapAggregatorOpportunity(o, idx) {
            return {
                id: 'live-' + idx,
                name: o.name,
                provider: o.provider,
                type: o.type,
                rate: o.rate,
                termDays: o.term_days,
                termDisplay: o.term_display,
                minimum: o.minimum,
                safetyRating: o.safety_rating,
                fdic: o.fdic_insured,
                govt: o.government_backed,
                liquidity: (o.liquidity || '').indexOf('High') === 0 || (o.liquidity || '') === 'Daily' ? 'High'
                          : (o.liquidity || '').indexOf('Low') === 0 ? 'Low' : 'Medium',
                source: o.source,
                isLive: !!o.is_live_data,
                asOf: o.as_of || null,
                stale: !!o.stale,
                notes: o.notes || ''
            };
        }

        function applyRateData(data, mode) {
            if (!data || !data.opportunities || !data.opportunities.length) return false;
            OPPORTUNITIES = data.opportunities.map(mapAggregatorOpportunity);
            DATA_STATUS = {
                mode: mode,
                generatedAt: data.generated_at || null,
                liveCount: data.live_data_count || 0,
                staleCount: data.stale_count || 0,
                warnings: data.warnings || []
            };
            return true;
        }

        async function loadLiveRates() {
            // 1) Rates injected server-side by the platform (freshest path)
            if (window.GOVSIGHT_RATES && applyRateData(window.GOVSIGHT_RATES, 'injected')) return;
            // 2) The platform API, when this page is served next to it
            try {
                const ctrl = new AbortController();
                const timer = setTimeout(() => ctrl.abort(), 6000);
                const resp = await fetch(API_BASE + '/api/investment/rates', { signal: ctrl.signal });
                clearTimeout(timer);
                if (resp.ok && applyRateData(await resp.json(), 'api')) return;
            } catch (e) { /* fall through to static */ }
            // 3) Static snapshot already in place
        }

        function dataStatusBanner() {
            const when = DATA_STATUS.generatedAt ? new Date(DATA_STATUS.generatedAt).toLocaleString() : null;
            if (DATA_STATUS.mode === 'static') {
                return '<div class="alert alert-warning"><strong>Static rate snapshot.</strong> ' +
                       'Live rates could not be loaded - every rate shown is a dated placeholder. ' +
                       'Do not make investment decisions from these figures.</div>';
            }
            const staleNote = DATA_STATUS.staleCount > 0
                ? ' ' + DATA_STATUS.staleCount + ' rate(s) are stale or staff-entered - check the badge on each card.'
                : '';
            return '<div class="alert alert-info">Rates loaded from the platform' +
                   (when ? ' at ' + when : '') + '.' + staleNote + '</div>';
        }

        function freshnessBadge(opp) {
            if (opp.isLive && !opp.stale) {
                return '<span class="badge badge-live">LIVE' + (opp.asOf ? ' · as of ' + opp.asOf : '') + '</span>';
            }
            if (opp.isLive && opp.stale) {
                return '<span class="badge badge-stale">LIVE (STALE' + (opp.asOf ? ' · ' + opp.asOf : '') + ')</span>';
            }
            if (opp.asOf) {
                return '<span class="badge badge-manual">STAFF-ENTERED · ' + opp.asOf + '</span>';
            }
            return '<span class="badge badge-static">STATIC ESTIMATE - VERIFY</span>';
        }

        function calculateReturn(opportunity, principal) {
            const rate = opportunity.rate / 100;
            const dailyRate = rate / 365;
            const interest = principal * dailyRate * opportunity.termDays;
            const totalReturn = principal + interest;
            const effectiveYield = (interest / principal) * (365 / opportunity.termDays) * 100;

            return {
                principal: principal,
                rate: opportunity.rate,
                termDays: opportunity.termDays,
                interestEarned: Math.round(interest * 100) / 100,
                totalReturn: Math.round(totalReturn * 100) / 100,
                effectiveYield: Math.round(effectiveYield * 1000) / 1000,
                opportunityName: opportunity.name
            };
        }

        // Switch tabs
        function switchTab(tabName) {
            // Update tab buttons
            const tabs = document.querySelectorAll('.tab');
            tabs.forEach(tab => tab.classList.remove('active'));
            event.target.classList.add('active');

            // Update content
            const contents = document.querySelectorAll('.tab-content');
            contents.forEach(content => content.classList.remove('active'));
            document.getElementById(tabName).classList.add('active');

            // Initialize comparison chart if switching to comparison tab
            if (tabName === 'comparison') {
                setTimeout(() => updateComparison(), 100);
            }
        }

        // Render opportunities
        function renderOpportunities() {
            const container = document.getElementById('opportunities');
            const liveCount = OPPORTUNITIES.filter(o => o.isLive).length;
            const estimatedCount = OPPORTUNITIES.filter(o => !o.isLive).length;

            let html = '<h2 style="margin-bottom: 20px">Available Investment Opportunities</h2>';
            
            html += dataStatusBanner();

            html += `
                <div class="metrics">
                    <div class="metric-card">
                        <div class="metric-label">Total Opportunities</div>
                        <div class="metric-value">${OPPORTUNITIES.length}</div>
                    </div>
                    <div class="metric-card">
                        <div class="metric-label">Live Data Sources</div>
                        <div class="metric-value">${liveCount}</div>
                        <div class="metric-delta">${liveCount > 0 ? 'Current' : 'No live data'}</div>
                    </div>
                    <div class="metric-card">
                        <div class="metric-label">Estimated Data</div>
                        <div class="metric-value">${estimatedCount}</div>
                        <div class="metric-delta">${estimatedCount > 0 ? 'Verify' : 'All live'}</div>
                    </div>
                </div>
            `;

            OPPORTUNITIES.forEach(opp => {
                html += `
                    <div class="opportunity-card ${!opp.isLive ? 'estimated' : ''}">
                        ${!opp.isLive ? `
                            <div class="alert alert-warning" style="margin-bottom: 16px">
                                
                                <div><strong>ESTIMATED RATE</strong> - This rate is not from live API data. Contact provider to verify current rates before investing.</div>
                            </div>
                        ` : ''}
                        
                        <div class="opportunity-header">
                            <div>
                                <div class="opportunity-title">
                                    ${!opp.isLive ? 'ESTIMATED: ' : ''}${opp.name}
                                </div>
                                <div style="margin-top: 8px; display: flex; gap: 8px">
                                    ${freshnessBadge(opp)}
                                    ${opp.fdic ? '<span class="badge badge-fdic">FDIC Insured</span>' : ''}
                                    ${opp.govt ? '<span class="badge badge-govt">Government Backed</span>' : ''}
                                </div>
                            </div>
                            <div class="opportunity-rate">${opp.rate}%</div>
                        </div>

                        <div class="opportunity-details">
                            <div class="detail-item">
                                <div class="detail-label">Provider</div>
                                <div class="detail-value">${opp.provider}</div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Type</div>
                                <div class="detail-value">${opp.type}</div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Term</div>
                                <div class="detail-value">${opp.termDisplay}</div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Minimum</div>
                                <div class="detail-value">$${opp.minimum.toLocaleString()}</div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Safety Rating</div>
                                <div class="detail-value">${opp.safetyRating}</div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Liquidity</div>
                                <div class="detail-value">${opp.liquidity}</div>
                            </div>
                        </div>

                        ${opp.notes ? `
                            <div style="margin-top: 16px; font-size: 13px; color: #5b6b7a">
                                ${opp.notes}
                            </div>
                        ` : ''}

                        <div style="margin-top: 16px; font-size: 12px; color: #9ca3af; font-style: italic">
                            Data source: ${opp.source}
                        </div>

                        ${opp.link ? `
                            <div style="margin-top: 16px">
                                <a href="${opp.link}" target="_blank" class="btn btn-primary" style="text-decoration: none; display: inline-block">
                                    ${opp.linkText} →
                                </a>
                            </div>
                        ` : ''}
                    </div>
                `;
            });

            container.innerHTML = html;
        }

        // Calculate best investment
        function calculateBest() {
            const principal = parseFloat(document.getElementById('calc-principal').value);
            const liquidity = document.getElementById('calc-liquidity').value;

            let filtered = [...OPPORTUNITIES];
            
            if (liquidity === 'high') {
                filtered = filtered.filter(o => o.liquidity.includes('Daily') || o.liquidity.includes('High'));
            } else if (liquidity === 'low') {
                filtered = filtered.filter(o => o.liquidity.includes('Low'));
            }

            filtered = filtered.filter(o => o.minimum <= principal);
            
            if (filtered.length === 0) {
                document.getElementById('calc-result').innerHTML = `
                    <div class="alert alert-warning" style="margin-top: 20px">
                        
                        <div>No suitable investments found for your criteria. Try adjusting the liquidity requirement or amount.</div>
                    </div>
                `;
                return;
            }

            const best = filtered.reduce((prev, curr) => curr.rate > prev.rate ? curr : prev);
            const calc = calculateReturn(best, principal);

            let html = `
                <div class="recommendation-box" style="margin-top: 30px">
                    ${!best.isLive ? `
                        <div class="alert alert-warning" style="margin-bottom: 16px">
                            
                            <div><strong>ESTIMATED RATE WARNING:</strong> The recommended investment uses an estimated rate. Verify current rates with the provider before proceeding.</div>
                        </div>
                    ` : ''}
                    
                    <h3>Recommended Investment Found!</h3>
                    <div style="font-size: 18px; opacity: 0.9; margin-bottom: 20px">
                        ${best.name}
                    </div>

                    <div class="recommendation-grid">
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Investment</div>
                            <div style="font-size: 20px; font-weight: 600">$${principal.toLocaleString()}</div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Rate</div>
                            <div style="font-size: 20px; font-weight: 600">${best.rate}%</div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Interest Earned</div>
                            <div style="font-size: 20px; font-weight: 600">$${calc.interestEarned.toLocaleString()}</div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Total Return</div>
                            <div style="font-size: 20px; font-weight: 600">$${calc.totalReturn.toLocaleString()}</div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Term</div>
                            <div style="font-size: 20px; font-weight: 600">${best.termDisplay}</div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Effective Yield</div>
                            <div style="font-size: 20px; font-weight: 600">${calc.effectiveYield.toFixed(2)}%</div>
                        </div>
                    </div>
                </div>
            `;

            document.getElementById('calc-result').innerHTML = html;
        }

        // Update comparison
        function updateComparison() {
            const amount = parseFloat(document.getElementById('compare-amount').value);
            const calculations = OPPORTUNITIES.map(opp => calculateReturn(opp, amount));

            // Update chart
            const ctx = document.getElementById('comparisonChart');
            
            if (comparisonChart) {
                comparisonChart.destroy();
            }

            comparisonChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: OPPORTUNITIES.map(o => o.name),
                    datasets: [{
                        label: 'Interest Earned ($)',
                        data: calculations.map(c => c.interestEarned),
                        backgroundColor: OPPORTUNITIES.map(o => 
                            o.isLive ? 'rgba(37, 99, 235, 0.8)' : 'rgba(245, 158, 11, 0.8)'
                        ),
                        borderColor: OPPORTUNITIES.map(o => 
                            o.isLive ? 'rgba(37, 99, 235, 1)' : 'rgba(245, 158, 11, 1)'
                        ),
                        borderWidth: 2
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            display: true,
                            position: 'top'
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            ticks: {
                                callback: function(value) {
                                    return '$' + value.toLocaleString();
                                }
                            }
                        },
                        x: {
                            ticks: {
                                maxRotation: 45,
                                minRotation: 45
                            }
                        }
                    }
                }
            });

            // Update table
            let tableHtml = `
                <h3 style="margin-bottom: 16px">Comparison Table</h3>
                <div style="overflow-x: auto">
                    <table>
                        <thead>
                            <tr>
                                <th>Investment</th>
                                <th>Rate</th>
                                <th style="text-align: right">Interest Earned</th>
                                <th style="text-align: right">Total Return</th>
                                <th style="text-align: center">Status</th>
                            </tr>
                        </thead>
                        <tbody>
            `;

            OPPORTUNITIES.forEach((opp, i) => {
                const calc = calculations[i];
                tableHtml += `
                    <tr>
                        <td>${opp.name}</td>
                        <td>${opp.rate}%</td>
                        <td style="text-align: right; font-weight: 600">$${calc.interestEarned.toLocaleString()}</td>
                        <td style="text-align: right; font-weight: 600">$${calc.totalReturn.toLocaleString()}</td>
                        <td style="text-align: center">
                            ${opp.isLive ? 
                                '<span class="badge badge-live">LIVE</span>' : 
                                '<span class="badge badge-estimated">ESTIMATED</span>'
                            }
                        </td>
                    </tr>
                `;
            });

            tableHtml += `
                        </tbody>
                    </table>
                </div>
            `;

            document.getElementById('comparison-table').innerHTML = tableHtml;
        }

        // Optimize grant
        function optimizeGrant() {
            const grantAmount = parseFloat(document.getElementById('grant-amount').value);
            const daysAvailable = parseInt(document.getElementById('grant-days').value);

            const suitable = OPPORTUNITIES.filter(o => 
                o.termDays <= daysAvailable && o.minimum <= grantAmount
            );

            if (suitable.length === 0) {
                document.getElementById('grant-result').innerHTML = `
                    <div class="alert alert-warning" style="margin-top: 20px">
                        
                        <div>No suitable investments found for your criteria. Try adjusting the days available or grant amount.</div>
                    </div>
                `;
                return;
            }

            const best = suitable.reduce((prev, curr) => curr.rate > prev.rate ? curr : prev);
            const calc = calculateReturn(best, grantAmount);

            const html = `
                <div class="recommendation-box" style="margin-top: 30px">
                    <h3>Grant Investment Optimized!</h3>
                    <div style="font-size: 16px; opacity: 0.9; margin-bottom: 20px">
                        Invest your grant funds in <strong>${best.name}</strong> for ${daysAvailable} days 
                        to earn <strong>$${calc.interestEarned.toLocaleString()}</strong> in additional revenue 
                        while maintaining safety and meeting expenditure deadline.
                    </div>

                    <div class="recommendation-grid">
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Grant Amount</div>
                            <div style="font-size: 20px; font-weight: 600">$${grantAmount.toLocaleString()}</div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Interest Rate</div>
                            <div style="font-size: 20px; font-weight: 600">${best.rate}%</div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Interest Earned</div>
                            <div style="font-size: 20px; font-weight: 600; color: #8fd6ac">
                                +$${calc.interestEarned.toLocaleString()}
                            </div>
                        </div>
                        <div>
                            <div style="font-size: 14px; opacity: 0.9">Total Return</div>
                            <div style="font-size: 20px; font-weight: 600">$${calc.totalReturn.toLocaleString()}</div>
                        </div>
                    </div>
                </div>
            `;

            document.getElementById('grant-result').innerHTML = html;
        }


        // ── Portfolio tracking ─────────────────────────────────────────────
        // API-backed when the platform backend is reachable; falls back to
        // localStorage so the tool still works on static hosting.
        let portfolioMode = 'local';
        let ladderChart = null;

        async function portfolioApi(path, options) {
            const resp = await fetch(API_BASE + path, options);
            if (!resp.ok) throw new Error('portfolio api ' + resp.status);
            return resp.json();
        }

        function localHoldings() {
            try { return JSON.parse(localStorage.getItem('gs_portfolio') || '[]'); }
            catch (e) { return []; }
        }
        function saveLocalHoldings(h) { localStorage.setItem('gs_portfolio', JSON.stringify(h)); }

        async function getHoldings() {
            try {
                const data = await portfolioApi('/api/portfolio');
                portfolioMode = 'api';
                return data.holdings;
            } catch (e) {
                portfolioMode = 'local';
                return localHoldings();
            }
        }

        function localAnalytics(holdings, idleCash, benchmarkRate) {
            const today = new Date();
            const total = holdings.reduce((s, h) => s + (+h.principal || 0), 0);
            const weighted = total ? holdings.reduce((s, h) => s + h.principal * h.rate, 0) / total : 0;
            const buckets = [
                ['Liquid (no maturity)', null, null], ['0-30 days', 0, 30], ['31-90 days', 31, 90],
                ['91-180 days', 91, 180], ['181-365 days', 181, 365], ['1+ years', 366, 100000]];
            const daysTo = h => h.maturity_date ? Math.round((new Date(h.maturity_date) - today) / 86400000) : null;
            const ladder = buckets.map(([label, lo, hi]) => ({
                bucket: label,
                principal: holdings.filter(h => {
                    const d = daysTo(h);
                    return lo === null ? d === null : (d !== null && d >= lo && d <= hi);
                }).reduce((s, h) => s + (+h.principal || 0), 0)
            }));
            const upcoming = holdings.map(h => ({ ...h, days_to_maturity: daysTo(h) }))
                .filter(h => h.days_to_maturity !== null && h.days_to_maturity <= 60)
                .sort((a, b) => a.days_to_maturity - b.days_to_maturity);
            return {
                total_principal: total, weighted_avg_yield: Math.round(weighted * 1000) / 1000,
                holdings_count: holdings.length, ladder: ladder, upcoming_maturities: upcoming,
                opportunity_cost: idleCash > 0 ? {
                    annual_cost: Math.round(idleCash * benchmarkRate) / 100,
                    monthly_cost: Math.round(idleCash * benchmarkRate / 12) / 100
                } : null
            };
        }

        async function renderPortfolio() {
            const idleCash = parseFloat(document.getElementById('idle-cash').value) || 0;
            const benchmarkRate = parseFloat(document.getElementById('benchmark-rate').value) || 0;
            const holdings = await getHoldings();
            let a;
            if (portfolioMode === 'api') {
                try {
                    a = await portfolioApi('/api/portfolio/analytics?idle_cash=' + idleCash +
                                           '&benchmark_rate=' + benchmarkRate);
                } catch (e) { a = localAnalytics(holdings, idleCash, benchmarkRate); }
            } else {
                a = localAnalytics(holdings, idleCash, benchmarkRate);
            }

            document.getElementById('portfolio-status').innerHTML = portfolioMode === 'api'
                ? '<div class="alert alert-info">Holdings stored on the GovSight platform.</div>'
                : '<div class="alert alert-warning">Platform API not reachable - holdings are stored in this browser only.</div>';

            document.getElementById('portfolio-metrics').innerHTML = `
                <div class="metric-card"><div class="metric-label">Total Invested</div>
                    <div class="metric-value">$${(a.total_principal || 0).toLocaleString()}</div></div>
                <div class="metric-card"><div class="metric-label">Weighted Avg Yield</div>
                    <div class="metric-value">${(a.weighted_avg_yield || 0).toFixed(2)}%</div></div>
                <div class="metric-card"><div class="metric-label">Holdings</div>
                    <div class="metric-value">${a.holdings_count || 0}</div></div>`;

            const oc = a.opportunity_cost;
            document.getElementById('opp-cost-result').innerHTML = oc
                ? `<div class="alert alert-warning"><strong>$${oc.annual_cost.toLocaleString()}/year</strong>
                   ($${oc.monthly_cost.toLocaleString()}/month) left on the table by keeping
                   $${idleCash.toLocaleString()} idle instead of at ${benchmarkRate}%.</div>`
                : '<div style="color:#666">Enter an idle cash amount to see the cost of not investing it.</div>';

            const alerts = (a.upcoming_maturities || []);
            document.getElementById('maturity-alerts').innerHTML = alerts.length
                ? '<div class="alert alert-warning"><strong>Reinvestment decisions needed:</strong><ul style="margin:6px 0 0 18px">' +
                  alerts.map(u => `<li>${u.description}: $${(+u.principal).toLocaleString()} matures ${u.maturity_date} (${u.days_to_maturity < 0 ? 'OVERDUE' : u.days_to_maturity + ' days'})</li>`).join('') +
                  '</ul></div>'
                : '';

            const rows = holdings.map(h => `
                <tr><td>${h.instrument_type}</td><td>${h.description}</td>
                <td style="text-align:right">$${(+h.principal).toLocaleString()}</td>
                <td style="text-align:right">${(+h.rate).toFixed(3)}%</td>
                <td>${h.purchase_date || ''}</td><td>${h.maturity_date || 'Liquid'}</td>
                <td><button class="button" style="padding:4px 10px" onclick="deleteHolding(${h.id ?? "'" + (h.local_id || '') + "'"})">Remove</button></td></tr>`).join('');
            document.getElementById('holdings-table').innerHTML = holdings.length
                ? `<table><thead><tr><th>Type</th><th>Description</th><th style="text-align:right">Principal</th>
                   <th style="text-align:right">Rate</th><th>Purchased</th><th>Matures</th><th></th></tr></thead>
                   <tbody>${rows}</tbody></table>`
                : '<div style="color:#666">No holdings recorded yet. Add the city\'s current investments below.</div>';

            const ladder = a.ladder || [];
            const ctx = document.getElementById('ladder-chart');
            if (ctx && typeof Chart !== 'undefined') {
                if (ladderChart) ladderChart.destroy();
                ladderChart = new Chart(ctx, {
                    type: 'bar',
                    data: {
                        labels: ladder.map(b => b.bucket),
                        datasets: [{ label: 'Principal maturing', data: ladder.map(b => b.principal),
                                     backgroundColor: '#2e6fa3' }]
                    },
                    options: { responsive: true, maintainAspectRatio: false,
                        plugins: { legend: { display: false } },
                        scales: { y: { ticks: { callback: v => '$' + (v / 1000000).toFixed(1) + 'M' } } } }
                });
            }
        }

        async function addHolding() {
            const h = {
                instrument_type: document.getElementById('h-type').value,
                description: document.getElementById('h-desc').value.trim(),
                principal: parseFloat(document.getElementById('h-principal').value),
                rate: parseFloat(document.getElementById('h-rate').value),
                purchase_date: document.getElementById('h-purchase').value,
                maturity_date: document.getElementById('h-maturity').value || null
            };
            if (!h.description || !h.principal || !h.rate || !h.purchase_date) {
                alert('Description, principal, rate, and purchase date are required.');
                return;
            }
            try {
                await portfolioApi('/api/portfolio', {
                    method: 'POST', headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(h) });
            } catch (e) {
                const all = localHoldings();
                h.local_id = 'L' + Date.now();
                all.push(h);
                saveLocalHoldings(all);
            }
            document.getElementById('h-desc').value = '';
            document.getElementById('h-principal').value = '';
            renderPortfolio();
        }

        async function deleteHolding(id) {
            if (typeof id === 'number') {
                try { await portfolioApi('/api/portfolio/' + id, { method: 'DELETE' }); }
                catch (e) { /* ignore */ }
            } else {
                saveLocalHoldings(localHoldings().filter(h => h.local_id !== id));
            }
            renderPortfolio();
        }

        // Initialize on load
        function __optimizerInit() {
            renderOpportunities();
            loadLiveRates().then(function() {
                renderOpportunities();
                if (document.getElementById('comparison').classList.contains('active')) updateComparison();
            });
            renderPortfolio();

        }

Object.assign(window, { addHolding, calculateBest, deleteHolding, optimizeGrant, renderPortfolio, switchTab });

export function mountInvestmentOptimizer(el) {
  el.innerHTML = BODY_HTML;
  __optimizerInit();
}
